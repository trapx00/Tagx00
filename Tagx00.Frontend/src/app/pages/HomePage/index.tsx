import React from "react";

export class HomePage extends React.Component<any, any> {
  render() {
    return "this is the true homepage and as you see, it doesn't share the same layout as other pages";
  }
}
