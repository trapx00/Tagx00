server:
  port: 8080
logging:
  level:
    org.springframework:
      data: DEBUG
      security: DEBUG
jwt:
  header: Authorization
  expiration: 604800
  tokenHead: "Bearer "
  route:
    authentication:
      register: "account/register"
      login: "account/login"